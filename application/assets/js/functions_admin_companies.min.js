function verify_acp_company(mode)
{
//	console.log(jQuery('#fileuploader_iframe').contents().find('.files .template-download .preview').html());
	
alert('test');
	var haserror = false;
	jQuery('#vehicle_registration').removeClass('error');
	jQuery('#vehicle_number').removeClass('error');
	jQuery('#vehicle_make').removeClass('error');
	jQuery('#vehicle_model').removeClass('error');
	jQuery('#vehicle_year').removeClass('error');
	jQuery('#request_date').removeClass('error');
	jQuery('#vehicle_value').removeClass('error');
	jQuery('#user_account').removeClass('error');
	jQuery('#usedfor').removeClass('error');

	if (jQuery('#subscriptionid').val().length == 0)
	{
		haserror = true;
		jQuery.growl.error({title: phrase['_error'], message: 'Please Select Subscription'});
		return false;
	}

	if (jQuery('#vehicle_registration').val().length <= 0)
	{
		haserror = true;
		jQuery('#vehicle_registration').addClass('error');
		jQuery.growl.error({title: phrase['_error'], message: 'Please enter the vehicle registration'});
		return false;
	}
	if (jQuery('#vehicle_number').val().length <= 0)
	{
		haserror = true;
		jQuery('#vehicle_number').addClass('error');
		jQuery.growl.error({title: phrase['_error'], message: 'Please enter the vehicle Number'}); 
		return false;
	}
	if (jQuery('#vehicle_make').val().length <= 0)
	{
		haserror = true;
		jQuery('#vehicle_make').addClass('error');
		jQuery.growl.error({title: phrase['_error'], message: 'Please enter the vehicle Make'}); 
		return false;
	}
	if (jQuery('#vehicle_model').val().length <= 0)
	{
		haserror = true;
		jQuery('#vehicle_model').addClass('error');
		jQuery.growl.error({title: phrase['_error'], message: 'Please enter the vehicle Model'}); 
		return false;
	}
	if (jQuery('#vehicle_year').val().length <= 0)
	{
		haserror = true;
		jQuery('#vehicle_year').addClass('error');
		jQuery.growl.error({title: phrase['_error'], message: 'Please enter the vehicle Year'}); 
		return false;
	}
	if (jQuery('#request_date').val().length <= 0)
	{
		haserror = true;
		jQuery('#request_date').addClass('error');
		jQuery.growl.error({title: phrase['_error'], message: 'Please enter the request date'}); 
		return false;
	}
	else
	{
		if (!check_dob(jQuery('#request_date').val()))
		{
			haserror = true;
			jQuery.growl.error({title: phrase['_error'], message: 'The request date does not appear valid.  Format: YYYY-MM-DD.  Minimum year is 1900.'});
			jQuery('#request_date').addClass('error');
			return false;
		}
	}
	if (jQuery('#vehicle_value').val().length <= 0)
	{
		haserror = true;
		jQuery('#vehicle_value').addClass('error');
		jQuery.growl.error({title: phrase['_error'], message: 'Please enter the value'}); 
		return false;
	}

	if (jQuery('#user_account').val().length <= 0)
	{
		haserror = true;
		jQuery('#user_account').addClass('error');
		jQuery.growl.error({title: phrase['_error'], message: 'Please enter customer account'}); 
		return false;
	}

	if (jQuery('#usedfor').val().length <= 0)
	{
		haserror = true;
		jQuery('#usedfor').addClass('error');
		jQuery.growl.error({title: phrase['_error'], message: 'Please enter used for value'}); 
		return false;
	}
	
	if (jQuery('#fileuploader_iframe').contents().find('.files .template-download .preview').html() == undefined)
	{
		haserror = true;
		jQuery.growl.error({ title: phrase['_error'], message: phrase['_please_upload_atleast_one_image'] });
	    smoothscroll('item-pictures');  
	    return false;
	}

	if (!haserror)
	{
		return true;
	}
	return false;
}

